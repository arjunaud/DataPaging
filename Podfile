# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PicSum' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PicSum
  pod 'Kingfisher', '~> 5.0'
  pod 'SDWebImage'
  pod 'Nuke', '~> 7.0'



  target 'PicSumTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PicSumUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
